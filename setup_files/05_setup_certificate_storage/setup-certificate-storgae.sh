#!/bin/bash

# Create a directory to hold the folders
sudo mkdir -p /opt/myapp/certificates

# Create the two subdirectories
sudo mkdir -p /opt/myapp/certificates/enabled_certificates
sudo mkdir -p /opt/myapp/certificates/disabled_certificates

